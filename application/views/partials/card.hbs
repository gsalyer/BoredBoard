<span class="img-card">
  <img src="{{this.thumbnail}}" alt="img not found :(" />
  <div class="img-info">
    <p>{{this.title}}</p>
    <p>{{this.description}}</p>
    <a href="/posts/{{this.id}}">Link</a>
  </div>
</span>